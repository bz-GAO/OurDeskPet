{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings-close",
  "description": "Allow the settings close-request handler to finish closing its own window.",
  "windows": ["settings"],
  "permissions": ["core:window:allow-destroy"]
}
